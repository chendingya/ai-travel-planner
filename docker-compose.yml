version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"

  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}